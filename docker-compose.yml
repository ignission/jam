version: "3.7"

services:
  server:
    container_name: jam-chat
    image: ignission/jam-chat:latest
    environment:
      JAVA_OPTIONS: -Dconfig.resource=local.conf
      OPENVIDU_SERVER_URL: https://kms:4443
    ports:
      - 8855:8855
    depends_on:
      - kms

  kms:
    container_name: jam-kms
    image: openvidu/openvidu-server-kms:2.14.0
    environment:
      openvidu.secret: MY_SECRET
    ports:
      - 4443:4443

  db:
    container_name: emoi-db
    image: mysql:5.7
    volumes:
      - db-data:/var/lib/mysql
      - ./docker/my.cnf:/etc/mysql/conf.d/my.cnf
    environment:
      - MYSQL_DATABASE=emoi
      - MYSQL_USER=emoi
      - MYSQL_PASSWORD=emoi
      - MYSQL_ROOT_PASSWORD=emoi
    ports:
      - 33055:3306

volumes:
  db-data:

networks:
  default:
    name: jam-network
