{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "docker-compose up -d",
            "type": "shell",
            "command": "cd ../ && docker-compose up -d"
        },
        {
            "label": "Start",
            "type": "shell",
            "command": "sbt \"~reStart --- -Dconfig.resource=local.conf\"",
            "group": "build",
            "dependsOn": [
                "docker-compose up -d"
            ]
        },
    ]
}